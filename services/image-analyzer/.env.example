# CLIP runs entirely locally — no OpenAI key needed.

# AWS — required for S3 image uploads (s3:PutObject on fp-property-images)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
